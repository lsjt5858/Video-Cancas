# AI 视频无限画布创作平台开发文档

> 项目暂定名：导演画布 / AI Director Canvas  
> 当前阶段：开发框架梳理  
> 文档版本：v1.1

## 1. 文档目的

这份文档是项目开发入口，用来统一产品范围、技术选型、模块边界和第一阶段开发顺序。

当前项目还没有代码结构，只有一份早期产品设想文档。原文档中有大量有价值的产品设想，但内容重复且偏立项说明，不适合直接指导开发。因此本次整理把它收敛成可执行的开发文档体系。

## 2. 文档目录

| 文档 | 作用 |
| --- | --- |
| [01-产品范围与MVP](docs/01-产品范围与MVP.md) | 明确做什么、不做什么，以及第一版闭环 |
| [02-系统架构](docs/02-系统架构.md) | 技术栈、服务划分、目录结构、任务流 |
| [03-功能模块设计](docs/03-功能模块设计.md) | 项目、画布、剧本、分镜、生成、剪辑、导出模块边界 |
| [04-数据模型与接口](docs/04-数据模型与接口.md) | 核心实体、状态机、数据库表、API 草案 |
| [05-开发计划与验收](docs/05-开发计划与验收.md) | 迭代顺序、里程碑、验收标准、风险清单 |
| [06-功能开发进度](docs/06-功能开发进度.md) | 功能开发清单、完成状态、下一步跟进 |

## 3. 产品一句话

本项目是一款面向 AI 视频创作者、短剧团队、自媒体工作室的无限画布式 AI 视频生产平台。

它要把分散在多个工具里的流程合并为一个结构化工作台：

```text
剧本 -> 分镜 -> 提示词 -> 角色/场景/道具 -> AI 生图 -> AI 生视频 -> 粗剪时间线 -> 导出
```

项目不是单纯的生图工具，也不是完整专业剪辑软件；第一阶段目标是跑通“从剧本到可导出视频素材包/MP4”的创作闭环。

## 4. 第一阶段 MVP 闭环

MVP 只做最短可验证链路：

```text
创建项目
  -> 输入或粘贴剧本
  -> AI 拆分场景和分镜
  -> 生成图片/视频提示词
  -> 在无限画布展示镜头节点
  -> 对单个镜头执行生图
  -> 从选中图片生成视频
  -> 视频回写到对应镜头
  -> 按分镜顺序生成粗剪时间线
  -> 导出素材包和 MP4
```

MVP 暂缓多人协作、复杂权限、模板市场、完整剪映草稿写入、高级视频特效和商业化计费。

## 5. 推荐技术框架

第一阶段建议采用前后端分离的轻量 monorepo：

```text
apps/web       React + Vite + TypeScript
apps/api       FastAPI + SQLAlchemy + PostgreSQL
apps/worker    Celery worker + AI Model Gateway + FFmpeg jobs
infra          Docker Compose / PostgreSQL / Redis / MinIO
docs           开发文档
```

核心技术选型：

| 层级 | 选型 | 原因 |
| --- | --- | --- |
| 前端 | React + Vite + TypeScript | 启动快，生态成熟，适合复杂交互应用 |
| 无限画布 | React Flow | 节点、连线、拖拽、缩放能力成熟 |
| 前端状态 | Zustand + TanStack Query | 本地画布状态和服务端数据分离 |
| 样式 | Tailwind CSS + shadcn/ui | 快速搭建一致的工具型界面 |
| 后端 API | FastAPI | AI/视频处理生态和 Python 任务链路更顺 |
| 数据库 | PostgreSQL | 结构化数据 + JSONB 扩展字段 |
| 异步队列 | Redis + Celery | 生图、生视频、导出都是长任务 |
| 文件存储 | MinIO/S3/OSS | 统一管理图片、视频、音频、导出包 |
| 视频处理 | FFmpeg | MP4 合成、转码、缩略图、字幕处理 |

## 6. 核心模块边界

| 模块 | 第一阶段职责 |
| --- | --- |
| 项目管理 | 创建项目、项目列表、项目基础配置 |
| 剧本模块 | 剧本文本编辑、AI 拆解为场景和分镜 |
| 分镜模块 | 分镜表、镜头字段编辑、提示词生成 |
| 无限画布 | 节点展示、拖拽、连线、节点属性编辑 |
| 资产库 | 管理图片、视频、音频、缩略图、来源关系 |
| AI 任务 | 统一封装 LLM、生图、生视频任务 |
| 时间线 | 按分镜顺序组装粗剪，不做专业剪辑 |
| 导出 | 导出素材包、分镜表、timeline.json、MP4 |

## 7. 开发优先级

1. 搭建 monorepo、Docker Compose、基础 API、数据库迁移。
2. 完成项目管理、剧本录入、分镜表 CRUD。
3. 完成无限画布的节点渲染、拖拽、连线、保存。
4. 接入 LLM，完成剧本拆分和提示词生成。
5. 接入图片生成任务，形成素材回写链路。
6. 接入视频生成任务，形成镜头视频回写链路。
7. 完成粗剪时间线和素材包/MP4 导出。

## 8. 第一阶段完成标准

第一阶段完成时，应该可以演示以下流程：

1. 用户创建一个短剧项目。
2. 用户粘贴一段剧本。
3. 系统生成 5-10 条结构化分镜。
4. 分镜以表格和画布节点两种方式展示。
5. 用户选择一个分镜生成多张图片。
6. 用户选中一张图片生成一段视频。
7. 视频素材自动关联到分镜。
8. 系统按分镜顺序生成粗剪预览。
9. 用户导出素材包和 MP4。

## 9. 当前下一步

建议下一步直接初始化代码框架：

```text
1. 创建 apps/web 的 React + Vite 项目
2. 创建 apps/api 的 FastAPI 项目
3. 创建 apps/worker 的 Celery worker
4. 创建 infra/docker-compose.yml
5. 建立 projects、scripts、shots、canvas_nodes、assets、ai_tasks 六张核心表
```

只要这部分搭好，后续所有功能都能按模块推进。
