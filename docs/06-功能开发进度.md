# 06 - 功能开发进度

> 更新时间：2026-06-14
> 状态标记：✅ 已完成 / 🟡 部分完成 / ❌ 未完成

## 1. 当前总体进度

| 模块 | 状态 | 当前说明 |
| --- | --- | --- |
| 项目文档框架 | ✅ | 已拆分产品范围、系统架构、功能模块、数据接口、开发计划 |
| Git 仓库初始化 | ✅ | 已初始化 Git，配置 `origin`，并推送到 GitHub |
| Monorepo 工程骨架 | ✅ | 已创建 `apps/web`、`apps/api`、`apps/worker`、`infra` |
| 前端原型迁移 | ✅ | 已将 `/Users/apple1/Downloads/AI Video Creation Tool` 的 Vite 前端迁入 `apps/web` |
| 前端依赖安装 | ✅ | 用户已在本机终端完成 `npm install`，已生成 `node_modules` 和 `package-lock.json` |
| 后端依赖安装 | ✅ | 已创建 `.venv`，并安装 API / worker 运行依赖和 API 测试依赖 |
| Docker 依赖服务启动 | ✅ | 已启动 PostgreSQL、Redis、MinIO，三个容器健康检查均通过 |
| MVP 业务闭环 | ❌ | 前端 localStorage 原型可演示，后端/API/生成任务尚未接入 |

## 2. 阶段 0：工程初始化

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| 创建基础 README | ✅ | 已写入项目说明和本地开发命令 |
| 初始化 Git 仓库 | ✅ | 已完成 `git init` 和首个提交 |
| 配置远程仓库 | ✅ | 已配置 `git@github.com:lsjt5858/Video-Cancas.git` |
| 推送 main 分支 | ✅ | 已推送到远程 `main` |
| 创建开发文档体系 | ✅ | 已新增 `docs/01` 到 `docs/05` |
| 创建根目录 `package.json` | ✅ | 已配置前端、API、worker、infra 常用脚本 |
| 创建 `.gitignore` | ✅ | 已忽略 Python、Node、环境变量、运行产物 |
| 创建 `.env.example` | ✅ | 已包含 API、数据库、Redis、MinIO、AI provider 配置 |
| 创建 `apps/web` 前端目录 | ✅ | 已创建 Vite + React + TypeScript 骨架 |
| 迁移第一版前端原型 | ✅ | 已迁入项目列表、工作台、剧本、分镜、画布、素材库、时间线 UI |
| 创建 `apps/api` 后端目录 | ✅ | 已创建 FastAPI 骨架 |
| 创建 `apps/worker` 队列目录 | ✅ | 已创建 Celery worker 骨架 |
| 创建 `infra/docker-compose.yml` | ✅ | 已配置 PostgreSQL、Redis、MinIO |
| 安装前端 npm 依赖 | ✅ | 用户已在本机终端完成安装 |
| 生成 `package-lock.json` | ✅ | 已生成 lockfile，后续依赖版本可复现 |
| 前端生产构建 | ✅ | `npm run build:web` 已通过 |
| 启动 Web 开发服务 | ✅ | `npm run dev:web` 已启动；因 5173 被占用，本次验证地址为 `http://127.0.0.1:5174/` |
| 浏览器页面验证 | ✅ | 已打开迁移后的页面，可创建项目并进入工作台，控制台无应用错误 |
| 启动 API 服务 | ✅ | `npm run dev:api` 已启动，`http://127.0.0.1:8000/health` 返回 `{"status":"ok","service":"api"}` |
| 启动 worker 服务 | ✅ | `npm run dev:worker` 已启动验证，Celery worker 可连接 `redis://localhost:6379/0` |
| 启动 Docker 依赖服务 | ✅ | `npm run infra:up` 已启动 PostgreSQL、Redis、MinIO，健康检查均为 healthy |

## 3. 阶段 1：项目、剧本、分镜

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| 项目列表 API | ✅ | 已接入 PostgreSQL，按创建时间倒序返回项目列表 |
| 创建项目 API | ✅ | 已接入 PostgreSQL，创建后写入 `projects` 表并返回持久化记录 |
| 项目详情 API | ✅ | 已接入 PostgreSQL，按 UUID 查询项目，不存在时返回 404 |
| 项目列表页面 | ✅ | 已接真实 API，刷新后从 PostgreSQL 读取项目列表 |
| 项目创建表单 | ✅ | 已接真实 API，创建后写入 PostgreSQL 并进入工作台 |
| 项目详情页面 | ✅ | 已迁入工作台页面，包含剧本、分镜、画布、素材库、时间线 Tab |
| 剧本保存 API | ✅ | 已实现 `PUT /api/projects/{project_id}/script`，首次创建当前剧本，后续覆盖并递增版本 |
| 剧本读取 API | ✅ | 已实现 `GET /api/projects/{project_id}/script`，可读取项目当前剧本 |
| 剧本编辑器 UI | ✅ | 已接真实 API，进入工作台读取当前剧本，保存时写入 PostgreSQL |
| 分镜 CRUD API | ✅ | 已实现 `scenes` / `shots` 数据表、迁移、项目子资源接口和 API 测试 |
| 分镜表 UI | ✅ | 已接真实 API，按项目读取 PostgreSQL 分镜 |
| 分镜字段编辑 | ✅ | 已接真实 API，编辑和删除会回写 PostgreSQL |
| 数据库迁移 | ✅ | 已接入 Alembic，并生成 `projects` 与 `scripts` 迁移 |
| PostgreSQL 持久化 | ✅ | `projects`、`scripts` 表已迁移，项目和当前剧本 API 已使用真实数据库读写 |

## 4. 阶段 2：无限画布

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| React Flow 基础接入 | ❌ | 迁入的第一版原型使用自定义 DOM 画布，后续再评估是否换回 React Flow |
| 画布基础视图 | ✅ | 已迁入支持缩放、平移和镜头卡片展示的画布原型 |
| 画布节点真实数据绑定 | ✅ | 已接 `canvas_nodes` API 和 PostgreSQL，前端画布按真实节点渲染 |
| 画布节点拖拽保存 | ✅ | 拖拽时本地即时更新，松手后 PATCH 节点位置到数据库 |
| 画布连线保存 | ✅ | 后端已实现 `canvas_edges` 创建和列表 API，前端可从节点端口拖拽创建 `story_flow` 连线并持久化 |
| 节点属性面板 | 🟡 | 右侧属性面板已接选中节点详情查看，场景节点和镜头基础字段可编辑并保存，其他节点编辑待补 |
| 分镜同步为节点 | ✅ | 读取画布节点时会为缺失的分镜自动创建 `shot` 节点 |
| 节点与分镜联动 | ✅ | 画布 `shot` 节点通过 `ref_id` 读取真实分镜内容 |
| 画布布局持久化 | ✅ | 节点位置已保存在 `canvas_nodes.position_x/position_y` |

### 4.1 画布补充待开发清单（参考小云雀画布）

小云雀类画布不是单纯的镜头卡片面板，而是“剧本联动 + 资产创作 + 生成编排”的工作区。当前项目已经完成镜头节点真实数据绑定，下面是需要继续补齐的能力。

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| 多类型节点展示 | 🟡 | 已展示 `script`、`scene`、`shot`、`character`、`location`、`prop`、`prompt` 等节点，`image_result`、`video_result`、`export` 待接 |
| 角色节点 | ✅ | 已从场景结构化角色字段自动生成 `character` 节点，并通过 `uses_asset` 连线关联相关场景 |
| 场景资产节点 | ✅ | 已从场景结构化地点字段自动生成 `location` 节点，并通过 `uses_asset` 连线关联相关场景 |
| 道具节点 | 🟡 | 已支持从场景/镜头文本中的显式 `道具：` / `props:` 标记生成 `prop` 节点，并通过 `uses_asset` 关联场景或镜头；LLM 自动解析待接 |
| 节点类型视觉区分 | 🟡 | 已为剧本、场景、镜头、角色、地点、道具、提示词节点提供不同标签、颜色和图标，其他节点类型待补 |
| 节点搜索与定位 | ✅ | 已支持按节点标题、场景名/地点/角色、镜头号/描述/提示词关键词搜索并自动定位到节点 |
| 画布小地图 | ✅ | 已增加 mini map，展示节点缩略布局和当前视口范围，支持点击快速导航大型画布 |
| 适应全部节点视图 | ✅ | 已支持一键缩放和平移到所有节点可见范围 |
| 画布缩放快捷键 | ✅ | 已支持滚轮缩放、快捷键缩放、重置视图和适配全部节点视图 |
| 框选节点 | ✅ | 已支持按住 Shift 在画布空白处拖拽框选多个节点，为批量操作做基础 |
| 多选节点操作 | 🟡 | 已支持多选后批量移动、批量删除和批量保存节点位置，分组、生成待补 |
| 节点对齐工具 | ✅ | 已支持多选节点左对齐、顶部对齐、水平分布和垂直分布，并批量保存节点位置 |
| 节点分组 | 🟡 | 已支持按场景和角色在画布中展示分组边框，并可在工具栏显示/隐藏；按集分组待剧集字段接入 |
| 节点折叠/展开 | ✅ | 场景节点已支持折叠/展开其下镜头和提示词节点，并同步隐藏相关连线、搜索、小地图和适配视图内容 |
| 节点右侧属性面板 | 🟡 | 点击节点后右侧展示视频创作业务字段，不再默认展示节点 ID/坐标等工程信息；场景字段和镜头编号、描述、景别、运镜、时长、台词、提示词已支持编辑保存 |
| 节点详情弹窗 | 🟡 | 已支持从属性面板打开弹窗查看场景/镜头完整创作信息，图片/视频结果以状态卡片展示并支持打开/复制链接，生成历史和多候选结果待真实数据接入 |
| 节点上下文菜单 | 🟡 | 右键节点已支持查看详情、复制节点信息和删除节点，生成图片/视频、创建关联节点待真实任务接入 |
| 画布空白区菜单 | 🟡 | 右键空白处已支持新增提示词、图片结果、视频结果、导出等纯画布节点，剧本/角色/场景业务节点待接 |
| 节点连线交互 | ✅ | 前端支持从节点端口拖出连线，并保存到 `canvas_edges` |
| 连线类型 | 🟡 | 已支持 `story_flow`、镜头到提示词的 `generates`、资产引用 `uses_asset` 关系，`selected_for_timeline` 业务链路待补 |
| 连线可视化 | ✅ | 已按关系类型区分 `story_flow`、`generates`、`uses_asset`、`selected_for_timeline` 的颜色和线型 |
| 剧本联动画布生成 | 🟡 | 读取画布时会自动生成剧本、场景、镜头、角色、地点、道具、提示词节点和基础链路，LLM 解析和生成结果链路待接 |
| 分镜批量生成节点 | ✅ | 已支持从剧本或场景节点右键批量生成镜头，创建后复用现有画布同步生成镜头节点 |
| 提示词节点生成 | ✅ | 读取画布时会为有提示词的镜头自动生成独立 `prompt` 节点，并通过 `generates` 连线关联镜头 |
| 生成模型选择器 | ✅ | 镜头/提示词节点属性面板已支持选择生图和生视频模型，包含快速、质量、角色一致性、场景一致性和运动增强等策略 |
| 生图参数面板 | ✅ | 镜头/提示词节点属性面板已支持画幅、风格、参考图模式、负向提示词、种子和候选数量参数 |
| 生视频参数面板 | ❌ | 支持时长、运镜、首帧、尾帧、参考视频、动作描述等参数 |
| 节点内生成按钮 | ❌ | 镜头/提示词节点卡片内直接提供“生图”“生视频”“重新生成”入口 |
| 生成进度显示 | ❌ | 节点内显示排队、生成中、成功、失败状态和进度 |
| 生成失败重试 | ❌ | 节点生成失败后可查看错误、修改参数并重试 |
| 图片候选节点 | ❌ | 一个镜头可生成多张图片候选，候选以子节点或折叠列表展示 |
| 视频候选节点 | ❌ | 一个镜头可生成多个视频候选，支持预览、替换、删除 |
| 结果选择机制 | ❌ | 从多个图片/视频候选中选择一个作为当前镜头主结果 |
| 结果回写分镜 | ❌ | 选择结果后回写 `shots.image_url` / `shots.video_url` 和对应资产引用 |
| 资产库拖入画布 | ❌ | 支持把素材库中的图片/视频/参考图拖入画布形成资产节点 |
| 画布节点加入素材库 | ❌ | 支持把生成结果节点保存到素材库，并保留来源节点和任务追溯 |
| 参考图绑定 | ❌ | 支持角色、场景、道具节点作为镜头生成的参考输入 |
| 角色一致性工作流 | ❌ | 角色节点可维护形象提示词、参考图、候选形象，并被镜头节点引用 |
| 场景一致性工作流 | ❌ | 场景节点可维护空间描述、参考图、全景图或多角度候选，并被镜头节点引用 |
| 批量选择生成 | ❌ | 可批量选择多个镜头节点，一次性提交生图或生视频任务 |
| 批量重新生成 | ❌ | 可对失败或不满意的节点批量重新生成 |
| 批量风格应用 | ❌ | 统一把项目风格、画幅、模型参数应用到一批节点 |
| 批量导出节点结果 | ❌ | 支持导出选中节点的图片、视频、提示词或分镜信息 |
| 画布版本快照 | ❌ | 保存画布布局和节点内容快照，后续支持回滚 |
| 画布自动布局 | 🟡 | 已按剧本 -> 场景 -> 镜头三列默认排布，提示词 -> 图片 -> 视频链路待补 |
| 画布性能优化 | ❌ | 大量节点时需要虚拟化、节流拖拽保存、局部渲染和懒加载缩略图 |
| 协作预留字段 | ❌ | 后续多人协作需要节点锁定、编辑人、更新时间和评论入口 |

## 5. 阶段 3：LLM 拆分和提示词

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| AI Model Gateway 目录 | ✅ | 已创建 `apps/worker/app/providers` |
| Provider 基类 | ✅ | 已创建 `BaseProvider` 占位 |
| LLM provider 适配器 | ❌ | 尚未实现 |
| 剧本分析任务 | ❌ | 尚未实现 |
| 剧本拆分分镜任务 | ❌ | 尚未实现 |
| 提示词生成任务 | ❌ | 尚未实现 |
| LLM JSON schema 校验 | ❌ | 尚未实现 |
| 任务状态 API | ❌ | 尚未实现 |
| WebSocket / 轮询进度 | ❌ | 尚未实现 |
| 失败重试 | ❌ | 尚未实现 |

## 6. 阶段 4：图片生成和素材库

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| 图片生成 provider | ❌ | 尚未实现 |
| 单镜头生图任务 | ❌ | 尚未实现 |
| 批量生图任务 | ❌ | 尚未实现 |
| 图片上传到对象存储 | ❌ | 尚未实现 |
| 缩略图生成 | ❌ | 尚未实现 |
| 素材库 API | ❌ | 尚未实现 |
| 素材库 UI | ✅ | 已迁入 localStorage 原型，后续需接素材 API 和对象存储 |
| 图片候选展示 | ❌ | 尚未实现 |
| 选择首帧图片 | ❌ | 尚未实现 |
| 图片结果回写画布节点 | ❌ | 尚未实现 |

## 7. 阶段 5：视频生成和时间线

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| 视频生成 provider | ❌ | 尚未实现 |
| 图生视频任务 | ❌ | 尚未实现 |
| 视频素材保存 | ❌ | 尚未实现 |
| 视频素材预览 | ❌ | 尚未实现 |
| 视频结果关联分镜 | ❌ | 尚未实现 |
| 时间线数据结构 | ❌ | 文档已有设计，代码未实现 |
| 自动生成粗剪时间线 | ❌ | 尚未实现 |
| 时间线 UI | ✅ | 已迁入 localStorage 原型 |
| clip 时长调整 | ❌ | 尚未验证和接真实时间线 API |

## 8. 阶段 6：导出

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| FFmpeg 工具目录 | ✅ | 已创建 `apps/worker/app/media` |
| FFmpeg 导出占位函数 | ✅ | 已创建 `build_preview_mp4` 占位 |
| 素材包导出任务 | ❌ | 尚未实现 |
| `storyboard.csv` 导出 | ❌ | 尚未实现 |
| `prompt_list.csv` 导出 | ❌ | 尚未实现 |
| `timeline.json` 导出 | ❌ | 尚未实现 |
| `subtitles.srt` 导出 | ❌ | 尚未实现 |
| MP4 合成 | ❌ | 尚未实现 |
| 导出中心 UI | ✅ | 已迁入时间线内的导出入口原型，真实导出任务未实现 |
| 导出任务失败记录 | ❌ | 尚未实现 |

## 9. 后续增强功能

| 功能 / 任务 | 状态 | 说明 |
| --- | --- | --- |
| 角色库 | ❌ | V1.5 规划 |
| 场景库 | ❌ | V1.5 规划 |
| 道具库 | ❌ | V1.5 规划 |
| 角色一致性提示词 | ❌ | V1.5 规划 |
| 故事板视图 | ❌ | V1.5 规划 |
| 九宫格结果对比 | ❌ | V1.5 规划 |
| 剪映草稿适配器 | ❌ | V2.0 规划 |
| 多人协作 | ❌ | V3.0 规划 |
| 团队权限 | ❌ | V3.0 规划 |
| 模板市场 | ❌ | V3.0 规划 |
| 模型额度计费 | ❌ | V3.0 规划 |

## 10. 下一步开发建议

阶段 0 运行基础已补齐，下一步进入阶段 1 的真实业务开发：

| 下一步 | 状态 |
| --- | --- |
| 完成 `npm install` 并生成 `package-lock.json` | ✅ |
| 启动 `apps/web` 并验证前端页面 | ✅ |
| 创建 Python 虚拟环境并安装 API / worker 依赖 | ✅ |
| 启动 Docker Compose 依赖服务 | ✅ |
| 验证 `/health` API | ✅ |
| 接入 Alembic 并生成第一版数据库迁移 | ✅ |

建议下一步进入阶段 2：无限画布节点真实数据绑定、拖拽位置持久化与连线保存。
